#ifndef HYDROLOGY_H
#define HYDROLOGY_H

#include <vector>

bool searchRainfallEvent(unsigned long *firstIndex, float *sumPrec, float *maxIntensity, int *lenght, std::vector<float> *prec);

#endif // HYDROLOGY_H
